# Bot runtime settings.
# Key=value format, lines starting with # are ignored.

# Maximum number of concurrent upload tasks (queued + processing).
# Requests beyond this limit are rejected immediately with an error message.
max_queue_size=5
